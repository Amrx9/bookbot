BookBot is a simple command-line application written in Python that performs static analysis on text files.
It returns the total number of characters mentioned in the text.  

This is also my first project with Boot.dev